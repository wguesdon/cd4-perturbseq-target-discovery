# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment / secrets
.env
.env.*

# Data (large and/or restricted — keep folder structure, ignore contents)
# data/external is reproduced exactly by scripts/fetch_priors.sh, so we ignore the
# payload rather than vendor ~10 MB of third-party tables.
data/raw/*
data/interim/*
data/processed/*
data/external/*
!data/raw/.gitkeep
!data/interim/.gitkeep
!data/processed/.gitkeep
!data/external/.gitkeep

# Single-cell / genomics artifacts (never commit these anywhere)
*.h5ad
*.h5
*.loom
*.zarr/
*.mtx
*.mtx.gz

# Notebook checkpoints
.ipynb_checkpoints/

# Quarto render output. report.html is 6 MB with resources embedded and is rebuilt by
# `quarto render reports/report.qmd`. Commit the final render at submission, not every draft.
/.quarto/
reports/*.html
reports/*_files/
reports/.jupyter_cache/

# OS cruft
.DS_Store

# Session log (kept local, not committed)
SESSION_SUMMARY.md

# Local-only working notes
*.local.md
